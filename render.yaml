services:
  - type: web
    name: aminalreport
    runtime: java
    plan: free

    buildCommand: ./mvnw clean package -DskipTests

    startCommand: java -jar target/AminalReport-0.0.1-SNAPSHOT.jar

    envVars:
      - key: JAVA_VERSION
        value: 21